import { BottomNavigationProps, Icon } from '@ui-kitten/components';

export const HomeIcon = (props: BottomNavigationProps) => <Icon {...props} name="home-outline" />;
export const PersonIcon = (props: BottomNavigationProps) => <Icon {...props} name="person-outline" />;
export const EmailIcon = (props: BottomNavigationProps) => <Icon {...props} name="mail-outline" />;
